## 1. Whether Inbox 탭을 구현한다. should really be checked, with justification

- Inbox 탭을 구현한다. should be checked only if Phase 9 lands successfully with both parts of the current TODO meaning satisfied at the
target SHA:
    - real delivered concerns are shown in Inbox
    - example concerns are shown when real items are insufficient
- Current-repo justification:
    - at af580e7d07199265aa0238404bfc297f1cc0b6bf, the Inbox tab already exists as a real delivery-backed route/list/detail flow
    - that earlier work is why the item was still unchecked: the remaining missing contract was the example-supply behavior
- So the rule for final TODO update is:
    - if Phase 9 completes the missing example-supply behavior without regressing the existing real Inbox flow, then the parent item Inbox
    탭을 구현한다. is now fully satisfied and should be checked
    - if the phase only adds partial plumbing or leaves the example-supply behavior incomplete, it must remain unchecked
- This is not a casual re-check of an old item; it is the completion of an item that was previously left open because one required sub-
behavior was still missing.

## 2. Pre-implementation verification checklist against the current SHA

Before any code edits, re-verify these assumptions against af580e7d07199265aa0238404bfc297f1cc0b6bf and adjust scope if any are false.

- Inbox is already delivery-backed and real-only today.
    - Verify in:
        - app/inbox/index.tsx
        - app/inbox/[deliveryId].tsx
        - src/features/inbox/api.ts
    - Expected finding:
        - Inbox currently reads concern_deliveries
        - detail and answer flow is keyed by delivery id
        - example supply is the missing extension, not a missing Inbox foundation
- Example answers can reuse the existing delivery-backed response flow.
    - Verify in:
        - src/features/responses/server/submit-response-service.ts
        - supabase/functions/submit-response/index.ts
        - supabase/migrations/20260419010000_phase8_notifications_push.sql
    - Expected finding:
        - submission logic is already delivery-based
        - no special assumption requires source_type = 'real' for response creation itself
- Notification suppression for example concerns is already enforced server-side.
    - Verify in:
        - public.submit_response_with_notifications_and_moderation_audit(...) in supabase/
        migrations/20260419010000_phase8_notifications_push.sql
    - Expected finding:
        - notification insert happens only when v_concern_source_type = 'real'
- Feedback blocking for example concerns is already enforced server-side.
    - Verify in:
        - private.enforce_real_concern_feedback() in supabase/migrations/20260417230000_mvp_db_foundation.sql
        - public.save_response_feedback_with_notifications(...) in supabase/migrations/20260419010000_phase8_notifications_push.sql
        - public.get_my_response_feedback_for_delivery(...) in supabase/migrations/20260419010000_phase8_notifications_push.sql
    - Expected finding:
        - DB trigger rejects feedback on non-real concerns
        - feedback RPC returns example_concern_not_allowed for example concerns
        - recipient-side feedback read only returns rows for source_type = 'real'
- Real routing no-delivery semantics are already separate from examples.
    - Verify in:
        - src/features/routing/server/route-concern-service.ts
        - src/features/routing/server/route-concern-service.test.ts
    - Expected finding:
        - routing only operates on sourceType = 'real'
        - zero eligible responders still returns real no_delivery
        - example concerns are rejected from routing rather than treated as fallback
- If any verification fails at the target SHA, the implementation plan narrows or changes before coding; I will not rely on intended
behavior that is not actually present in code.

## 3. Final TODO.md check list

Check only after implementation if each item is fully true in code at the end of the phase.

- 예제 고민은 실제 사용자 고민처럼 보이게 표시하되 내부적으로 \example`로 태깅한다.`
- Inbox 탭을 구현한다.
    - Check only because the current repo already has the real Inbox tab and this phase completes the still-missing example-supply part of
    that same TODO item.
- 예제 고민은 UI상 실제 사용자 고민처럼 보이게 처리하고 별도 시각 분리 라벨은 넣지 않는다.
- 예제 고민에는 feedback UI와 feedback API를 열지 않는다.
- 예제 고민 seed 데이터를 준비한다.
- 예제 고민을 \concerns.source_type = example`로 저장한다.`
- 예제 고민은 실제 사용자 고민처럼 보이게 \Inbox`에서 노출한다.`
- 예제 고민은 실사용자 고민 라우팅의 fallback branch로 사용하지 않는다.
- 실제 고민의 적격 풀이 비어 있어 no-delivery가 발생해도, 이는 별도의 routing 결과로 유지하고 예제 고민 공급과 혼동하지 않는다.
- 예제 고민 테스트를 작성한다.

## 4. Final TODO.md leave-unchecked list

Leave these unchecked even if Phase 9 preserves or tests part of their intent.

- 예제 고민은 답변만 가능하고 좋아요/후기/푸시/solved-count/기본 분석 대상에서 제외한다.
- 예제 고민은 push/feedback/solved-count/기본 분석 대상에서 제외한다.
- solved-count는 저장형 카운터가 아니라 실사용자 고민의 실제 positive feedback으로부터 파생 계산한다.
- solved-count는 \real concern + positive feedback`만 집계하도록 구현한다.`
- 기본 분석에서 example concern 이벤트를 분리 태깅한다.

Strict rationale for final TODO editing:

- Phase 9 will implement and verify these narrower truths:
    - example concerns can be answered
    - example concerns create no notification/push side effects
    - example concerns remain blocked from feedback server-side and hidden client-side
- But the broader umbrella TODO lines above imply full repository-level exclusion semantics across:
    - push
    - feedback
    - solved-count
    - baseline/default analytics
- Because solved-count and baseline/default analytics do not yet exist as implemented repository features, checking those umbrella lines
now would over-claim completion under docs/RULE.md.
- Final TODO updates must therefore reflect only what this phase actually completes, not the larger future-exclusion intent of those
umbrella lines.

## 5. A short statement confirming the plan is aligned with the current codebase, not just the intended design

- This plan is anchored to the current repository at af580e7d07199265aa0238404bfc297f1cc0b6bf: it assumes only behaviors that are first
re-verified in the existing Inbox, routing, response, notification, and feedback code paths, and it treats any failed assumption as a
scope change before implementation rather than as an implicit dependency.
